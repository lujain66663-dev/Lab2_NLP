# Lab2_NLP

This lab covers basic text preprocessing techniques in NLP using Regular Expressions, NLTK, and spaCy. The tasks include extracting hashtags from Twitter data, using re.compile() and re.split(), and tokenizing text with spaCy and NLTK.
